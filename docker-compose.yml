version: "3.9"

services:
  backend:
    build: ./backend/PAS.API
    ports:
      - "5000:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend